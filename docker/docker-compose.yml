services:
  stt:
    image: ghcr.io/speaches-ai/speaches:latest-cuda
    container_name: babel-tower-stt
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    ports:
      - "29000:8000"
    volumes:
      - stt-models:/home/ubuntu/.cache/huggingface/hub
    environment:
      - WHISPER__MODEL=${BABEL_STT_MODEL:-Systran/faster-whisper-large-v3}
      - WHISPER__COMPUTE_TYPE=int8
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  daemon:
    build:
      context: ..
      dockerfile: docker/Dockerfile.app
    container_name: babel-tower-daemon
    command: ["python", "-m", "babel_tower.cli", "daemon"]
    environment:
      - PULSE_SERVER=unix:/tmp/pulse.socket
      - BABEL_STT_URL=http://stt:8000
      - BABEL_STT_HOTWORDS=${BABEL_STT_HOTWORDS:-}
      - BABEL_STT_PROMPT=${BABEL_STT_PROMPT:-}
      - BABEL_STT_CORRECTIONS=${BABEL_STT_CORRECTIONS:-}
      - BABEL_LLM_URL=http://ai-station:4000
      - BABEL_DEFAULT_MODE=structure
      - WAYLAND_DISPLAY=${WAYLAND_DISPLAY:-wayland-0}
    volumes:
      - ${XDG_RUNTIME_DIR:-/run/user/1000}/pulse/native:/tmp/pulse.socket
      - ${XDG_RUNTIME_DIR:-/run/user/1000}/${WAYLAND_DISPLAY:-wayland-0}:/tmp/${WAYLAND_DISPLAY:-wayland-0}
    user: "${UID:-1000}:${GID:-1000}"
    depends_on:
      stt:
        condition: service_healthy
    restart: unless-stopped

  mcp:
    build:
      context: ..
      dockerfile: docker/Dockerfile.app
    container_name: babel-tower-mcp
    command: ["python", "-m", "babel_tower.cli", "mcp", "--transport", "sse", "--port", "3000"]
    network_mode: host
    user: "${UID:-1000}:${GID:-1000}"
    environment:
      - PULSE_SERVER=unix:/tmp/pulse.socket
      - BABEL_STT_URL=http://localhost:29000
      - BABEL_STT_HOTWORDS=${BABEL_STT_HOTWORDS:-}
      - BABEL_STT_PROMPT=${BABEL_STT_PROMPT:-}
      - BABEL_STT_CORRECTIONS=${BABEL_STT_CORRECTIONS:-}
      - BABEL_LLM_URL=http://ai-station:4000
      - BABEL_LLM_MODEL=${BABEL_LLM_MODEL:-babel}
      - BABEL_LLM_API_KEY=${BABEL_LLM_API_KEY}
      - BABEL_TTS_ENABLED=${BABEL_TTS_ENABLED:-false}
      - DBUS_SESSION_BUS_ADDRESS=unix:path=/tmp/bus
      - HOME=/tmp
    volumes:
      - ${XDG_RUNTIME_DIR:-/run/user/1000}/pulse/native:/tmp/pulse.socket
      - ${XDG_RUNTIME_DIR:-/run/user/1000}/bus:/tmp/bus
    restart: unless-stopped

volumes:
  stt-models:
